#### math

1. perm(n, m)   求排列 `A(n, m) = n! / (n-m)!`
2. comb(n, m)   求组合 `C(n, m) = n! / ((n-m)! * m!)`
3. factorial(n)   求阶乘



#### itertools

1. permutations(iterator, m)   求排列
2. combinations(iterator, m)   求组合