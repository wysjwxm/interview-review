# Interview Review — 面试复盘与改进

个人项目,用于对技术面试录音进行复盘、分析并生成改进建议。

## 目录结构

```
.
├── resume/     # 个人简历(通常为 PDF),用于了解候选人背景、技术栈、项目经历
├── audio/      # 面试录音(原始音频),以及由录音生成的同名转录文本
├── improve/    # 由转录文本生成的复盘总结文档(与音频同名)
└── CLAUDE.md   # 本文件
```

## 标准工作流程

每次处理一段面试录音时,按以下三步执行。

### 第 1 步:读取并分析简历

- 读取 `./resume/` 下的 PDF(用 Read 工具直接读,支持 PDF)。
- 提取并记住:姓名、求职方向、技术栈、项目经历、工作年限等背景信息。
- 这些信息用于在复盘时判断「哪些问题答得好/答得差、哪里是简历没写清导致的追问」。

### 第 2 步:音频转文字

> ⚠️ `ffmpeg` 只能做音频格式转换,**不能**做语音识别。转写需要 whisper 类工具。

1. **预处理(ffmpeg)**:把录音转成 whisper 偏好的格式 —— 16kHz、单声道、16-bit WAV:
   ```bash
   ffmpeg -i <原始音频> -ar 16000 -ac 1 -c:a pcm_s16le <同名>.wav
   ```
2. **转写(whisper)**:用 whisper 把 WAV 转成带时间戳/说话人可选的文本。可选工具(均未安装,首次使用前需先安装):
   - `whisper.cpp`(推荐,macOS 上 `brew install whisper-cpp`,用 `whisper-cli`)
   - `openai-whisper`(`pip install openai-whisper`,注意系统 Python 3.9 需用 venv 或 `--user`)
3. **保存文本**:输出到 `./audio/`,**与原音频同名**(仅扩展名改为 `.txt`),即「声明是文本」。
   - 例:`interview1.m4a` → `interview1.txt`
   - 中间产生的临时 `.wav` 处理完后删除。

### 第 3 步:生成复盘总结文档

- 读取第 2 步生成的转录文本,结合简历背景,生成总结文档。
- 保存到 `./improve/`,**与音频同名**(扩展名 `.md`)。
- 例:`interview1.m4a` → `./improve/interview1.md`

## 复盘总结文档必须包含的章节

按以下顺序输出,使用 Markdown:

1. **问答概要** —— 按时间/话题顺序梳理面试中问了什么、候选人怎么答的、结果如何。
2. **面试缺点** —— 暴露出的问题(如表达不清、紧张、答非所问、基础知识薄弱等)。
3. **可改进点** —— 针对上述缺点的具体、可操作改进措施。
4. **缺少的知识点** —— 面试中明显暴露出的、候选人尚未掌握或答错的知识点。
5. **下一步建议学习的知识点** —— 结合目标岗位和简历,列出优先学习清单(可按优先级排序)。

## 约定与注意事项

- 目录名以 `improve` 为准(勿写成 `impreve`)。
- 所有输出文档使用**中文**,技术术语保留英文。
- 转录文本是复盘的事实依据;总结要具体到「哪个问题、哪句话、哪里答偏了」,避免空泛。
- 若录音包含多个说话人(面试官 + 候选人),尽量在转录/总结中区分。
- 尊重隐私:简历与录音中的个人信息仅用于本项目内的复盘分析,不外发。
